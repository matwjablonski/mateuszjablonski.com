import styled from 'styled-components';
import { th } from '@smooth-ui/core-sc';

export const BlogDescriptionBox = styled.p`
  text-align: center;
  color: ${th('lightGrey')};
  transform: translateY(-10px);
  text-transform: uppercase;
  font-family: ${th('mainFont')};
  font-weight: 700;
`;
