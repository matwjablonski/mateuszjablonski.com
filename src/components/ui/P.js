import styled from 'styled-components';
import { th } from '@smooth-ui/core-sc';

const P = styled.p`
  font-family: ${th('mainFont')};
  font-size: 14px;
  line-height: 1.6;
  padding-bottom: 35px;
`;

export default P;
